pipeline_steps:
  paring:
    step:
      _target_: mts.pipeline.step.pair.cv.make_orb_distance_parer
      num_features: 2048
      k: 50
      distance_th: 1000
      max_pairs_per_image: 20
      max_pairs: 1000
      top_n: 600
