pipeline_steps:
  paring:
    step:
      _target_: mts.pipeline.step.pair.cv.make_fast_distance_parer
      threshold: 10
      nonmax_suppression: true
      num_features: 1024
      k: 40
      distance_th: 1000
      max_pairs_per_image: 20
      max_pairs: 1000
      top_n: 600
