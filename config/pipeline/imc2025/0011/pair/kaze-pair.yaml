pipeline_steps:
  paring:
    step:
      _target_: mts.pipeline.step.pair.cv.make_kaze_distance_parer
      threshold: 0.001
      n_octaves: 4
      k: 20
      distance_th: 1000
      max_pairs_per_image: 10
      max_pairs: 500
      top_n: 300
