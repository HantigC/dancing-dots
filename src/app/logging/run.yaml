version: 1
formatters:
  formatter:
    format: '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
handlers:
  console:
    class: logging.StreamHandler
    level: INFO
    formatter: formatter
    stream: ext://sys.stdout
loggers:
  mts:
    level: INFO
    handlers: [console]
    propagate: no
  app:
    level: INFO
    handlers: [console]
    propagate: no
root:
  level: WARNING
  handlers: [console]